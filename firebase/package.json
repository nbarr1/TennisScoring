{
  "name": "@tennis/firebase-functions",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --external:firebase-admin --external:firebase-admin/app --external:firebase-admin/firestore --external:firebase-admin/messaging --external:firebase-functions --external:firebase-functions/v2 --external:@tennis/shared --external:pdf-lib --outfile=lib/index.js --format=cjs",
    "dev": "tsc --watch",
    "serve": "firebase emulators:start --only functions,firestore,auth,storage",
    "deploy": "firebase deploy --only functions",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts"
  },
  "main": "lib/index.js",
  "engines": {
    "node": ">=20 <23"
  },
  "dependencies": {
    "@tennis/shared": "workspace:*",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "typescript": "^6.0.0",
    "@types/node": "^20.0.0"
  }
}
